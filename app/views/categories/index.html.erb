<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

<div class='text-center'>
<%= button_to "Add a new category", new_category_path, class: 'btn btn-primary btn-lg active', method: :get %>
</div>
