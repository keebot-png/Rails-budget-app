<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= link_to category.name, category_path(category.id) %>
  </p>

  <p>
    <strong>Icon:</strong>
    <%= category.icon %>
  </p>

</div>
